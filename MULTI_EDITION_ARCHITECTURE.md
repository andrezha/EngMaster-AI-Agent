# 多版本架构（第一阶段）

当前单一代码库注册五个学习级别，但 V1.0 只开放高考英语正式版：

- `zhongkao`：初中英语（30词体验；正式版开发中）
- `gaokao`：高考英语（完整正式数据；V1.0 已开放购买）
- `cet4`：大学英语四级（30词体验；正式版开发中）
- `cet6`：大学英语六级（30词体验；正式版开发中）
- `kaoyan`：考研英语（30词体验；正式版开发中）

正式程序默认仍启动 `gaokao`，现有高考用户的数据目录保持不变。
另外三个版本的数据分别保存在 `editions/<edition_id>` 下，错词、进度和
自主登记不会相互覆盖。

开发环境可以使用以下任一种方式指定版本：

```powershell
python main.py --edition zhongkao
python main.py --edition cet4
python main.py --edition cet6
python main.py --edition kaoyan
```

或者：

```powershell
$env:ENGMASTER_EDITION = "cet4"
python main.py
```

未激活时直接进入分级体验中心。版本管理会显示全部五个级别：高考正式版
显示为“未解锁”或绿色“已解锁”，其余四个正式版统一显示“开发中”，不能
购买或进入。命令行和记忆设置也不能绕过正式版发布状态与权限检查。

页面由 `edition_config.py` 中的 `enabled_pages` 控制。五个版本都显示
单词闯关、词汇表、自主登记、短语/不规则动词闯关和对应数据表。
短语与不规则动词数据路径也按版本配置；目前开发阶段五个版本暂时复用
高考、四级、六级和考研暂时共用现有126条不规则动词。初中版已接入
从两份初中词库筛选并精简生成的90条不规则动词，以及90条常用短语候选数据；
其余版本短语后续可以分别替换而不改页面代码。

授权使用离线签名的 EM3 权限码。V1.0 只签发高考英语权限；数据结构保留未来
累计增加正式版本的能力。旧 EM2 激活码继续有效，并按原单一产品视为高考英语
权限。体验版不需要激活，五类体验的数据分别保存。

为了验证单程序多版本切换，当前开发包会携带四份30词样例；正式发布前需要
替换为来源确认后的正式词库。
