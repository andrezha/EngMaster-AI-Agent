# 词库校正报告

校正对象：`assets/vocabulary.json`（3,875 条）和 `assets/irregular_verbs.json`（126 条）  
校正日期：2026-07-17

## 校正结果

- 完成 235 条可审计的词库修正，涵盖错词、错误大小写、错误/串行音标、音标字段错位、词组被拆分和少量相邻释义误复制。
- 修正 20 项不规则动词数据，包括 15 组原形/过去式/过去分词错列，以及 5 项明显错误释义。
- 校正后词条数量保持不变：普通词库 3,875 条，不规则动词表 126 条。
- 校正后 `word` 字段中不再残留音标，所有音标方括号均配对。

## 代表性音标修正

| 序号 | 词条 | 原错误 | 修正后 |
|---:|---|---|---|
| 303 | available | 误用 autumn 的音标 | `[əˈveɪləb(ə)l]` |
| 1394 | fly | 误用 fluent 的音标 | `[flaɪ]` |
| 2384 | park | 误用 Paris 的音标 | `[pɑːk]` |
| 2466 | pile | `[paɪ]` 且音标粘入词头 | `[paɪl]` |
| 2473 | ping-pong | 尾音误写为 `/ɡ/` | `[ˈpɪŋ pɒŋ]` |
| 2909 | scratch | 缺少开头 `/s/` | `[skrætʃ]` |
| 3012 | should | 误用 shot 的音标 | `[ʃʊd]` |
| 3332 | Swiss | 误用 swing 的音标 | `[swɪs]` |
| 3380 | telephone | 误用 telegraph 的音标 | `[ˈtelɪfəʊn]` |
| 3675 | vinegar | 误用 villager 的音标 | `[ˈvɪnɪɡə(r)]` |
| 3692 | volleyball | 误用 volcano 的音标 | `[ˈvɒlɪbɔːl]` |
| 3718 | warn | 原词头错写为 war | `[wɔːn]` |

同类串行错误还包括 `consume/dozen/honey/menu/persuade/pianist/press/pretend/side/size/speak/spend/these/tidy/tobacco/today/together/tortoise/unhappy/unknown/visit/wash/wood/zone` 等。

## 拼写、大小写和字段修正

- 错词修正示例：`acchor → anchor`、`afte → after`、`cassettle → cassette`、`commericial → commercial`、`Tibeta → Tibetan`、`taxipayer → taxpayer`。
- 普通词恢复小写：`Baby`、`Cheer up`、`Dynamic`、`Dynasty`、`Indicate`、`Mosquito`、`Scratch`。
- 专有名词和缩写继续保留正确大写，例如 `China`、`English`、`AIDS`、`DVD`。
- 拆开的词组已合并为真实词头，例如 `according to`、`telephone booth`、`bus stop`、`Hong Kong`、`New Zealand`、`table tennis`。
- `result` 在原始 JSON 中本来就是小写，并仍保持为 `result [rɪˈzʌlt]`。

## 不规则动词修正

主要修正包括：

- `be — was/were — been`
- `burn — burned/burnt — burned/burnt`
- `hang — hung/hanged — hung/hanged`
- `have — had — had`
- `learn — learned/learnt — learned/learnt`
- `show — showed — shown/showed`
- `sink — sank/sunk — sunk`
- `strike — struck — struck/stricken`
- 区分 `lay — laid — laid`（放置）与 `lie — lay — lain`（躺、位于）。

## 程序显示问题

界面此前对正确答案调用 `capitalize()`，会把 `result` 临时显示为 `Result`，并导致正确输入小写 `result` 时被判错。该显示与判定逻辑已改为保留词库原始大小写，并在比较时做大小写无关的规范化。

完整迁移规则保存在 `tools/repair_word_data.py`，便于以后复核每项修改。

## 短语表校正（2026-07-17）

- 原短语表600条，删除21组重复项和8条批量生成的非标准 `dynamic` 搭配，整理为571条不同短语。
- 清除了210条例句中的模板词 `dynamic`，其中包括45处 `dynamic dynamic` 明显重复。
- 修正了 `take count of → take account of`、`come power → come to power`、`get an upper hand → get the upper hand`、`set an example to → set an example for` 等词形错误。
- 将 `cure/inform/prevent/provide/remind/separate` 等短语补全为带 `sb/sth` 的规范结构。
- 替换了残留“布局流程、绳索套路、公交车常规”等不自然例句，并同步校正中文翻译。
- 短语闯关现在支持多种正确表达，例如 `get on`/`get off`、`as if`/`as though`、`make sure`/`make certain`，不再要求输入JSON中的斜杠合并形式。

短语迁移规则保存在 `tools/repair_phrase_data.py`。
